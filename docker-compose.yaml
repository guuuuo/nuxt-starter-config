version: '3.8'

services:
  app:
    container_name: nuxt-starter
    image: cr.amoylabs.com:30443/guuuuo/nuxt-starter:7
    restart: always
    
networks:
  default:
    external: true
    name: proxy-net