apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sfe4j
spec:
  ingressClassName: nginx
  rules:
  - host: sfe4j.amoylabs.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sfe4j
            port: 
              number: 8080
